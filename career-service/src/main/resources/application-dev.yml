# Local development profile — convenient schema sync and verbose SQL/diagnostics.
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    com.careeros: DEBUG
    org.hibernate.SQL: DEBUG
