# auth-service — owns database `careeros_auth` on its dedicated PostgreSQL instance.
# Shared, profile-independent configuration is imported from careeros-common.
spring:
  application:
    name: auth-service
  config:
    import: "classpath:application-common.yml"
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/careeros_auth}
    username: ${DB_USERNAME:careeros}
    password: ${DB_PASSWORD:careeros}
    hikari:
      pool-name: auth-cp
      maximum-pool-size: ${DB_POOL_MAX:10}
      minimum-idle: ${DB_POOL_MIN:2}

server:
  port: ${SERVER_PORT:8081}
