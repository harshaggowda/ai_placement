/**
 * Declarative OpenFeign clients for downstream services (currently the FastAPI AI service).
 *
 * <p>Scanned by {@link com.careeros.aiorchestrator.config.FeignClientConfig}. Holds interface-based
 * HTTP clients only; imperative client wiring (RestClient, WebClient) lives under {@code config}.
 */
package com.careeros.aiorchestrator.client;
