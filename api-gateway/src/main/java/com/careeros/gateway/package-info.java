/**
 * CareerOS AI — api-gateway.
 *
 * <p>Single public entry point built on reactive Spring Cloud Gateway. Routing is configured in
 * {@code application.yml}; {@code config} and {@code filter} are reserved for future custom global
 * filters (edge JWT validation, rate limiting, request enrichment).
 */
package com.careeros.gateway;
